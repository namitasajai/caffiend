import React from "react";
import { View, Text } from "react-native";
import ReviewCard from "../components/FriendPostCard";

const SocialScreen = () => (
  <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
    <ReviewCard />
  </View>
);

export default SocialScreen;
